<!-- Create a virtual Environment by using - python -m venv venv on terminal in selected folder

Activate that virtual environment by using this - venv\Scripts\activate 

Now install the requirements by using this - pip install -r requirements.txt 

write the logic for tools to accept the symptom to return the matching symptom solution else display match not found

Create a MCP server and register our tool then start the server by using  python server.py -->